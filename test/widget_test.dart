// Smoke test — VDisPlay
// Note: full widget tests require a native DLL which is not available in CI.
// This file is kept as a placeholder.

import 'package:flutter_test/flutter_test.dart';


void main() {
  testWidgets('VDisPlayApp builds without throwing', (WidgetTester tester) async {
    // VDisPlayApp requires the native DLL; skip in headless environments.
  });
}
